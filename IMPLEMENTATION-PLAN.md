# Implementation Plan — 12 Steps / 12 Prompts

**Status:** v2.0 — 2026-09-16 (**re-planned for the Go + SQLite pivot** — DECISIONS D-65/D-66/D-67. v1.x history: v1.2 moved Docker Compose to the backlog (D-63) and put the MVP on SQLite via Prisma (D-64); the Node/Express work done under v1.x was reverted in commit `c9336b3` and the plan restarts at Step 1.)
**Deadline:** 2026-09-18
**Spec:** [VALIDATION-OF-REQUIREMENTS.md](./VALIDATION-OF-REQUIREMENTS.md) · **Detailed specs:** [DECISIONS.md](./DECISIONS.md) · **Agent rules:** [CLAUDE.md](../CLAUDE.md)

Every prompt below implicitly starts with: _"Follow `my-docs/DECISIONS.md` for all behavioral details (D-xx references); where this prompt and DECISIONS.md differ, DECISIONS.md wins."_

Each step is one Claude Code session/prompt and should end in one or more small, coherent commits (made by the user in VS Code — Claude proposes the boundaries, D-61). The order is deliberate: it produces the commit story the brief says evaluators look for (§7.3 — scaffolding first, features one by one with tests alongside, bonus, then polish/docs) and, per the stakeholder's instruction, gets the **data model and sample data in place first** so every later step has real data to work with.

Every step's prompt ends with the same two standing instructions, so they're not repeated in each box:

- _"Append an entry to `my-docs/AGENT-COLLABORATION-HISTORY.MD` for this prompt."_
- _"Call out commit boundaries as you go; don't leave everything for one commit at the end."_

Rough budget: ~24 working hours of the remaining window. Steps 1–6 (Go backend on the in-memory store) by end of day 1 (2026-09-16); 7–10 (frontend + bonus) by end of day 2; 11 (SQLite) and 12 (E2E + polish) on day 3 with margin. **Step 11 is not optional** — the brief requires a relational database (D-66).

## The two phases (D-66)

|                             | Phase 1 — Steps 2–10                                                                                  | Phase 2 — Step 11 onward                                                                                                      |
| --------------------------- | ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| Store                       | `store/memory`, loaded from `server/data/sample.json` at boot; writes are in-process, restart = reset | `store/sqlite` (`database/sql` + `modernc.org/sqlite`), schema from embedded `schema.sql`, seeded from the same `sample.json` |
| `STORE` default             | `memory`                                                                                              | `sqlite`                                                                                                                      |
| Tests                       | Go integration tests on the memory store                                                              | The same tests on both stores + one conformance suite                                                                         |
| What changes between phases | Nothing in handlers, services, validation, or the client                                              | —                                                                                                                             |

## Progress

| Step                                                                | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Log                                                 |
| ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------- |
| 0 — Pivot: docs + re-scaffold                                       | ✅ Done 2026-09-16                                                                                                                                                                                                                                                                                                                                                                                                                                                 | [History Entry 9](./AGENT-COLLABORATION-HISTORY.MD) |
| 1 — Scaffolding (Go + React)                                        | ✅ Done 2026-09-16 — Go installed, `go build`/`go test`/`gofmt`/`go vet` all verified clean; committed (`42bcbce`, `8970872`)                                                                                                                                                                                                                                                                                                                                      | Entry 9                                             |
| 2 — Domain model + sample data + memory store                       | ✅ Done 2026-09-16 — verified (`go build`/`go test`/`gofmt` clean, coverage 93 %+ with `-coverpkg`); awaiting commit                                                                                                                                                                                                                                                                                                                                               | This entry                                          |
| 3 — Custom authentication                                           | ✅ Done 2026-09-17 — verified (`go build`/`go test`/`gofmt`/`go vet` clean, coverage 93.5 %+); awaiting commit                                                                                                                                                                                                                                                                                                                                                     | This entry                                          |
| 4 — User profiles + follow graph                                    | ✅ Done 2026-09-17 — verified (`go build`/`go test`/`gofmt`/`go vet` clean, coverage 93.6 %+); awaiting commit                                                                                                                                                                                                                                                                                                                                                     | This entry                                          |
| 5 — Tweets, timeline, likes                                         | ✅ Done 2026-09-18 — verified (`go build`/`go test`/`gofmt`/`go vet` clean, coverage 93.1 %+); awaiting commit                                                                                                                                                                                                                                                                                                                                                     | This entry                                          |
| 6 — User search + coverage checkpoint                               | ✅ Done 2026-09-18 — verified (`go build`/`go test`/`gofmt`/`go vet` clean, coverage 93.4 % ≥ 85 % floor, recorded in README); awaiting commit                                                                                                                                                                                                                                                                                                                     | This entry                                          |
| 7 — Frontend foundation + auth UI + first frontend integration test | ✅ Done 2026-09-18 — verified (`tsc`/`vite build`/ESLint clean, client test suite green, and the register→login→logout loop driven in a real headless browser at 375/768/1280px against the real Go API); awaiting commit                                                                                                                                                                                                                                          | This entry                                          |
| 8 — Timeline, compose tweet, delete, like (frontend + test)         | ✅ Done 2026-09-18 — verified (`tsc`/`vite build`/ESLint/Prettier clean, client test suite green (4/4), and compose/like/delete driven in a real headless browser against the real Go API and alice's real sample timeline at 375/768/1280px); awaiting commit                                                                                                                                                                                                     | This entry                                          |
| 9 — Profile pages, follow/unfollow, search (frontend + test)        | ✅ Done 2026-09-18 — verified (`tsc`/`vite build`/ESLint/Prettier clean, client test suite green (6/6), and profile/follow/unfollow/bio-edit/followers-list/search driven in a real headless browser against the real Go API and alice's real sample follow graph at 375/1280px); awaiting commit                                                                                                                                                                  | This entry                                          |
| 10 — Bonus: reply threads (backend + frontend + tests)              | ✅ Done 2026-09-18 — verified (`go build`/`go vet`/`gofmt`/`go test` clean, coverage 93.2 % ≥ 85 % floor; `tsc`/`vite build`/ESLint/Prettier clean, client test suite green (8/8); a multi-level thread created and navigated in a real headless browser against the real Go API at 375/1280px, including the D-49 deleted-ancestor placeholder); awaiting commit                                                                                                  | This entry                                          |
| 11 — SQLite store, seed command, switch the default                 | ✅ Done 2026-09-18 — verified (`go build`/`go vet`/`gofmt`/`go test` clean; `storetest` conformance suite green on `store/sqlite`; the full `internal/httpapi` integration suite green on both `STORE=memory` and `HTTPAPI_TEST_STORE=sqlite`; coverage 91.5 % ≥ 85 % floor; a fresh clone's `npm run dev` auto-seeds `server/data/twitter.db` and a restart persists data created through the API, verified against the real binary with `curl`); awaiting commit | This entry                                          |
| 12 — E2E, responsive QA, coverage/docs                              | ✅ Done 2026-09-18 — Playwright auth spec passing, responsive QA found/fixed two real bugs (a11y nav labels, a touch target), coverage still 91.5 % ≥ 85 % floor, Runbook re-verified                                                                                                                                                                                                                                                                              | This entry                                          |

Pre-pivot history (for the record): under v1.x, Step 1 (Node scaffold) was committed and Step 2 (Prisma on SQLite + Vitest infra) was completed and verified locally but never committed; both were reverted by the user in `c9336b3` when the pivot was decided. Entries 4–8 in the history log document that work.

---

## Step 1 — Scaffolding: Go API + React client + tooling + CI

**Goal:** the "first commit: project scaffolding" the brief expects, with both apps bootable, lint/format wired, and CI green from the first push.

**Prompt:**

> Scaffold the project per the layout in CLAUDE.md. `server/`: a Go module (`github.com/jdavidrt/theflock-twitter-clone/server`, `go 1.23`) with `cmd/api/main.go` (loads the root `.env` via `internal/config`, builds the handler, `http.Server` with sane timeouts and graceful shutdown on SIGINT/SIGTERM), `internal/config` (typed `Config` from env — `PORT`, `APP_ENV`, `JWT_SECRET` ≥ 32 chars, `COOKIE_SECURE` — plus the small dotenv loader; D-43), and `internal/httpapi` (`NewHandler` on the Go 1.22 `http.ServeMux`, `GET /api/health` → `200 {"status":"ok"}`, a JSON 404 for unknown `/api/*` routes in the D-52 envelope, and middlewares: recover → 500, request log via `log/slog`, security headers, 16 kB body limit — D-55/D-57). Tests with `testing` + `httptest` for config parsing, the dotenv loader, health, 404 envelope, recover and body limit. `client/`: Vite + React 18 + TypeScript + plain hand-written CSS (mobile-first, D-68) + React Router + TanStack Query with a placeholder home page and a Vite dev proxy `/api` → `http://localhost:3000` (D-42). Root: `package.json` with the `client` workspace and scripts `dev` (concurrently: `cd server && go run ./cmd/api` + Vite), `build`, `lint` (ESLint + `go vet`), `format`/`format:check` (Prettier + `gofmt`), `test`, `test:coverage` (D-32/D-45); `.nvmrc`, `.editorconfig`, `.gitattributes` (`* text=auto eol=lf`), ESLint + Prettier config, `.env.example` with exactly the Step 1 variables from D-43. `.github/workflows/ci.yml` per D-37 (server: setup-go, gofmt check, vet, `go test -race` + coverage ≥ 85 %; client: `npm ci`, lint, format check, build, test). README Runbook updated to the real commands.

**Done when:** `cp .env.example .env && npm install && npm run dev` serves the client at 5173 and `curl localhost:3000/api/health` returns 200; `npm test` and `npm run lint` are green; CI passes. Two-plus commits on `main` (tooling / server / client / CI).

**Rubric:** Development process, Code quality.

_As executed (Entry 9, 2026-09-16):_ all files written as part of the pivot session; client verified (`npm install`, lint, build); the Go side could not be compiled because Go was not yet installed on the dev machine — verify with `cd server && go build ./... && go test ./...` before committing.

---

## Step 2 — Domain model, sample data, `Store` interface, in-memory store

**Goal:** the whole data model and a realistic dataset exist before any feature — the stakeholder's "very first step" — so every later endpoint is built and tested against real data.

**Prompt:**

> In `server/`, add `internal/domain` with the entity structs from VALIDATION-OF-REQUIREMENTS.md §7 (`User` with `DisplayName`, `Bio`, `PasswordHash`, no avatar — D-12; `Follow` keyed by `(FollowerID, FolloweeID)`; `Tweet` with `Content`, nullable `ParentTweetID` and `DeletedAt` — D-16/D-47; `Like` keyed by `(UserID, TweetID)`), UUID string ids (D-51), `time.Time` UTC timestamps, explicit camelCase `json` tags. Define `internal/store.Store` (D-66): the interface every service will need — users (create, get by id/username/email, update profile, search), follows (create/delete idempotently, list followers/following with cursor, counts, `IsFollowing`), tweets (create, get, soft-delete, list by author, timeline for a viewer, replies of a tweet, ancestors), likes (create/delete idempotently, count, `LikedBy`), and the cursor type from D-19 (`createdAt|id`, base64url). Sentinel errors (`ErrNotFound`, `ErrConflict`, `ErrSelfFollow`). Write `server/data/sample.json` per D-67 (12 users starting with alice, plain `password` fields, fixed UUIDs and RFC 3339 timestamps, 8–20 top-level tweets each, follows 3–8 each with alice ≥ 6, cross-likes, ~30 replies some nested) and `internal/store/sample` — a loader that parses + validates the file (uniqueness, references, no self-follow, D-13 length) and hashes each distinct password once with bcrypt at the configured cost. Implement `internal/store/memory` (maps + `sync.RWMutex`, every list ordered per D-17/D-19/D-24/D-48, soft-deleted rows excluded everywhere). Add `internal/store/storetest`: a conformance suite (`func Run(t *testing.T, newStore func(t *testing.T) store.Store)`) covering every interface method including pagination stability and soft-delete exclusion, and run it from `memory`. Unit-test the loader against the real `sample.json`. Wire `STORE` (default `memory`) and `SAMPLE_DATA_PATH` (default `./data/sample.json`) into `internal/config`, `.env.example`, D-43's table and the README; `cmd/api` builds the store at boot and logs the counts loaded. Add a temporary read-only `GET /api/health` detail or a log line showing the loaded counts — no feature endpoints yet.

**Done when:** `npm run dev` logs "loaded 12 users, N tweets, N follows, N likes"; `go test ./...` runs the conformance suite green on the memory store; coverage stays ≥ 85 %. Commits: domain + store interface → sample data + loader → memory store + conformance suite.

**Rubric:** Code quality, Testing, Seed requirement (§5.2).

_As executed (2026-09-16):_ `internal/domain` (User/Follow/Tweet/Like structs, explicit camelCase JSON tags, `Tweet.IsReply`/`IsDeleted` helpers). `internal/store` (the `Store` interface exactly as scoped in the prompt, sentinel errors, the `Cursor` type with `EncodeCursor`/`DecodeCursor`/`Before`/`After`, `Page[T]`). One deliberate interface decision beyond the prompt: `Ancestors` does **not** exclude soft-deleted tweets (every other list/read method does) — documented in the `Store` doc comment as an exception needed so the Step 10 thread page can render the D-49 "this tweet was deleted" placeholder; `storetest` asserts both halves of that split. `server/data/sample.json` generated deterministically (12 users, 169 tweets [139 top-level + 30 replies, some two levels deep], 63 follows [alice: 7], 499 cross-likes, no self-follows/self-likes/dangling references — verified by a script, not committed) via a throwaway Node generator in the scratch directory, per D-69's "genuinely bulk/mechanical" exception. `internal/store/sample` (loader: parses, validates referential integrity and D-13 content limits with all errors joined in one pass, hashes each distinct password once, topologically reorders tweets so parents insert before children regardless of file order). `internal/store/memory` (maps + `sync.RWMutex`, generic cursor pagination helpers, `createdAt DESC, id DESC` / `createdAt ASC, id ASC` ordering per D-19). `internal/store/storetest` (conformance suite covering every interface method, idempotency, soft-delete exclusion, and pagination no-dupes/no-gaps across pages), run today from `store/memory`. Wired `STORE` (default `memory`) and `SAMPLE_DATA_PATH` (default `./data/sample.json`) into `internal/config` (plus a new `Config.BcryptCost()` per D-06), `.env.example`, the local `.env`, and the README; `cmd/api` loads the store at boot and logs the counts (not yet wired into the handler — no feature endpoints exist until Step 3). Found and fixed a real regression while verifying: `internal/store/storetest` has no test file of its own (its functions are called from `store/memory`'s test file), so the CI/`npm run test:coverage` command as written in D-32 measured it at 0 % and dragged total coverage from 93 % to 44 %; fixed by adding `-coverpkg=./internal/...` to both the npm script and the CI workflow, documented as a D-32 amendment. Verified end-to-end: `go build ./... && go vet ./... && gofmt -l .` clean; full suite green; `go test ./internal/... -coverpkg=./internal/... -coverprofile -covermode=atomic` → 93.2 % total statements; ran the real API (`go run ./cmd/api`) and confirmed the boot log (`loaded sample data store=memory users=12 tweets=169 follows=63 likes=499`) and `GET /api/health` still return 200.

---

## Step 3 — Custom authentication (backend + tests)

**Goal:** the hard requirement the brief singles out — fully custom auth, no third-party providers.

**Prompt:**

> Implement custom authentication in `server/`: `POST /api/auth/register` (email, username, password, optional displayName — rules per D-01…D-05 including lowercase normalization and the reserved-username list; bcrypt cost 12, cost 4 when `APP_ENV=test` — D-06), `POST /api/auth/login` (email only, generic 401 on failure — D-07/D-08), `POST /api/auth/logout`, `GET /api/auth/me`. Session = HS256 JWT (`golang-jwt/jwt/v5`) in an httpOnly `token` cookie, SameSite=Lax, 7-day expiry, `Secure` from `COOKIE_SECURE` (D-09). Add `internal/validation` (pure functions, per-field errors → D-52 `details`; D-54), `internal/service/auth`, the `requireAuth` middleware that puts the user id in the request context, a central `writeError` producing the D-52 envelope for every error path, a per-IP `x/time/rate` limiter on login/register (D-55, off when `APP_ENV=test`), and JSON-only body enforcement on state-changing routes (D-56). Layering per D-53. Tests: unit tests for the validation rules (username regex/bounds, reserved list, email normalization, password bounds), the code-point counter with a multi-byte emoji case (D-13), JWT sign/verify round-trip and expiry; `httptest` integration tests on the memory store covering successful register/login/logout/me, duplicate email/username → 409, reserved username → 400, wrong password → 401, protected route without cookie → 401, malformed JSON / wrong content-type → 400, cookie attributes.

**Done when:** all auth tests pass; `go test ./internal/... -cover` is visible and ≥ 85 %.

**Rubric:** Functionality (auth), Testing, Code quality.

_As executed (2026-09-17):_ `internal/validation` (pure functions per D-54: `Username`/`Email`/`Password`/`DisplayName` return the normalized value plus a per-field error list, `CountCodePoints` is the shared `utf8.RuneCountInString` helper both password bounds and the future D-13 tweet-length check will use, reserved-username list from D-04, username/email regexes). `internal/service/auth` (`Service.Register`/`Login`/`IssueToken`/`VerifyToken`; `*ValidationError`/`*ConflictError` sentinel types carrying D-52 `details`; `ErrInvalidCredentials` for the D-08 generic 401; bcrypt cost and JWT secret injected from `config.Config`; ids via `github.com/google/uuid`, JWTs via `github.com/golang-jwt/jwt/v5` HS256 with an explicit signing-method check in `VerifyToken` so an `alg=none` token is rejected). `internal/httpapi`: `auth.go` (`handleRegister`/`handleLogin`/`handleLogout`/`handleMe`), `cookie.go` (D-09 cookie: `HttpOnly`, `SameSite=Lax`, `Path=/`, `Secure` from `COOKIE_SECURE`, 7-day expiry / `MaxAge=-1` to clear), `context.go` (`requireAuth` middleware + request-context user id), `decode.go` (JSON body decode → D-52 `VALIDATION_ERROR` on malformed input), `ratelimit.go` (per-IP `golang.org/x/time/rate` limiter, 20 req/15 min, scoped to `/api/auth/{login,register}` only, off under `APP_ENV=test` — D-55), and a `jsonOnly` middleware added to `middleware.go` (D-56: state-changing requests must carry `Content-Type: application/json`). `Deps` gained a `Store` field; `cmd/api` now builds the store once and passes it into `httpapi.NewHandler` instead of discarding it after boot-time loading. Dependency note: `go get`/`go mod tidy` kept bumping `go.mod`'s `go` directive to the locally installed 1.26+ toolchain (triggered by `golang.org/x/time`'s latest version requiring go 1.26); pinned `golang.org/x/time@v0.9.0` instead and hand-set `go 1.23.0` back so the module still declares the D-65 minimum. Tests: `internal/validation` (table-driven per field, including an emoji multi-byte case for `CountCodePoints`/`Password`), `internal/service/auth` (register success/validation/reserved-name/duplicate-email/duplicate-username, login success/wrong-password/unknown-email, JWT round-trip/garbage/wrong-secret/expired/`alg=none`), `internal/httpapi` (full register→me→logout→me flow, duplicate email/username → 409 with field details, reserved username → 400, wrong password and unknown email → the same generic 401, protected routes without a cookie → 401, a token for a since-deleted user → 401, malformed JSON → 400, wrong/missing `Content-Type` → 400, cookie attribute assertions, an unexpected store error → 500 with no leaked detail, and per-IP rate-limit exhaustion/isolation/APP_ENV=test bypass). Verified end-to-end: `go build ./... && go vet ./... && gofmt -l .` clean; full suite green; `go test ./internal/... -coverpkg=./internal/... -coverprofile -covermode=atomic` → 93.5 % total statements; ran the real API (`go run ./cmd/api`) and drove register/me/login (with the README's `alice@example.com`/`Password123!` sample credentials)/logout/unauthorized-me/reserved-username/duplicate-email/wrong-content-type with `curl`, confirming the responses and `Set-Cookie` headers match D-09.

---

## Step 4 — User profiles + follow graph (backend + tests)

**Prompt:**

> Add user and follow endpoints: `GET /api/users/{username}` (profile with tweet/follower/following counts computed on read — D-23 — and `isFollowedByMe`), `PATCH /api/users/me` (displayName 1–50, bio ≤ 160 — D-12), `POST`/`DELETE /api/users/{username}/follow` (idempotent, self-follow → 400 — D-21), `GET /api/users/{username}/followers` and `/following` (cursor-paginated per D-19, newest first — D-24). No avatar field (D-12). Integration tests for each endpoint including: follow twice is idempotent, unfollow when not following is a no-op, self-follow rejected, pagination cursor returns no duplicates/gaps across the sample data, unknown username → 404.

**Done when:** tests green; the follow graph can be built and queried entirely via the API on top of the sample data.

**Rubric:** Functionality (profile, follows, followers lists), Testing.

_As executed (2026-09-17):_ `internal/service/social` (`Service.GetProfile`/`UpdateProfile`/`Follow`/`Unfollow`/`ListFollowers`/`ListFollowing`; a `Profile` view struct carrying counts computed on read via `TweetCount`/`FollowerCount`/`FollowingCount`/`IsFollowing` (D-23) plus the viewer's follow state; `*ValidationError` for D-12 field errors; `ErrSelfFollow` checked before the store is touched, per the D-21 amendment). Added `validation.Bio` (0-160 code points, trimmed) alongside the existing D-54 functions. `internal/httpapi`: `users.go` (`handleGetProfile`/`handleUpdateMe`/`handleFollow`/`handleUnfollow`/`handleFollowList`, a `profileResponse` shape that deliberately omits email — not a public profile field per D-12 — and a `followListResponse` reusing the D-19 cursor envelope) and a new `pagination.go` (`parseCursorAndLimit`, shared by every cursor-paginated endpoint from here on). Routes wired behind `requireAuth` per D-11: `GET /api/users/{username}`, `PATCH /api/users/me`, `POST`/`DELETE /api/users/{username}/follow`, `GET /api/users/{username}/{followers,following}`. Tests: `internal/service/social` (profile counts/follow-state, case-insensitive username lookup, profile update validation, follow idempotency, self-follow rejection, unfollow no-op, followers-list follow-state per item, unknown-username → `ErrNotFound`); `internal/httpapi` (all of the above through the real HTTP layer plus unknown-username → 404 for profile/follow/followers, no-cookie → 401, invalid cursor/limit → 400, followers/following ordered newest-first with a small real-clock sleep between actions so the assertion isn't racing `time.Now()` resolution, and an `explodingTweetCountStore` mirroring Step 3's `explodingEmailStore` pattern to prove an unexpected store error surfaces as a 500 with no leaked detail). One dedicated test loads the real `sample.json` and walks `GET /api/users/alice/followers` a page at a time (limit 3 against alice's 11 real sample followers) to assert no duplicates/gaps against an unpaginated ground-truth fetch, per this step's explicit pagination requirement. Verified end-to-end: `gofmt -l .` clean; `go build ./...`/`go vet ./...` clean; full suite green across 5 repeated runs (no flakes); `go test ./internal/... -coverpkg=./internal/... -coverprofile -covermode=atomic` → **93.6 % total statements** (up from 93.5 % after Step 3); ran the real binary and drove profile/follow/unfollow/followers/PATCH-me with `curl` against the seeded sample data, confirming counts, idempotency, the generic self-follow 400, and the 404/401 paths. Mid-session process note: `my-docs/IMPLEMENTATION-PLAN.md` (this file) was relocated to the repo root during the session — outside the `my-docs/` gitignore rule — per explicit user decision, so the build plan ships as public process evidence; the other `my-docs/` files (`DECISIONS.md`, `VALIDATION-OF-REQUIREMENTS.md`, `AGENT-COLLABORATION-HISTORY.MD`) remain gitignored. `CLAUDE.md`'s reference to this file was updated accordingly.

---

## Step 5 — Tweets, timeline, likes (backend + tests)

**Prompt:**

> Add tweet endpoints: `POST /api/tweets` (content 1–280 code points, trimmed, newlines preserved — D-13/D-14), `DELETE /api/tweets/{id}` (author only → 403 otherwise; **soft delete** via `deletedAt`, excluded by every store list method — D-16), `GET /api/tweets/{id}` (404 if deleted), `GET /api/users/{username}/tweets` (top-level, not deleted, cursor-paginated). Timeline: `GET /api/timeline` returns top-level tweets from followed users plus the viewer's own (D-17), `createdAt DESC, id DESC`, cursor-paginated per D-19, each tweet including an author summary, `likeCount`, `replyCount`, and `likedByMe`. Likes: `POST`/`DELETE /api/tweets/{id}/like` (idempotent, own tweets allowed, deleted tweet → 404 — D-22). Counts on read (D-23); note that choice in README trade-offs. Integration tests: 281-code-point rejected (multi-byte emoji case), empty rejected, delete by non-author → 403, deleted tweet disappears from timeline/profile and returns 404 by id, timeline excludes non-followed users' tweets and excludes replies, timeline ordering, pagination has no duplicates/gaps, like twice counts once, unlike removes. Use alice's sample timeline as a fixture where it makes assertions concrete.

**Done when:** tests green; a timeline can be produced end to end via the API.

**Rubric:** Functionality (tweets, timeline, pagination, likes, counter), Testing.

_As executed (2026-09-18):_ the store-level tweet/like methods (`CreateTweet`, `GetTweet`, `SoftDeleteTweet`, `ListTweetsByAuthor`, `Timeline`, `Like`, `Unlike`, `LikeCount`, `LikedByMe`, `ReplyCount`) were already implemented and conformance-tested back in Step 2, so this step was the service and HTTP layers on top of them. `internal/service/tweet` (`Service.Create`/`Get`/`Delete`/`ListByUsername`/`Timeline`/`Like`/`Unlike`; a `View` DTO — tweet + author + `likeCount`/`replyCount`/`likedByMe` computed on read, mirroring `social.Profile`'s shape; `*ValidationError` for bad content; `ErrForbidden` for a non-author delete). `validation.TweetContent` (1-280 code points after trimming outer whitespace, internal newlines preserved — D-13/D-14). `internal/httpapi/tweets.go`: `handleCreateTweet` (`POST /api/tweets`), `handleGetTweet` (`GET /api/tweets/{id}`, 404 on missing/deleted), `handleDeleteTweet` (`DELETE /api/tweets/{id}` → 204), `handleListUserTweets` (`GET /api/users/{username}/tweets`), `handleTimeline` (`GET /api/timeline`), and one `handleLikeAction` factory shared by `POST`/`DELETE /api/tweets/{id}/like` (same shared-factory pattern Step 4 used for followers/following). `writeTweetError` takes a `notFoundMessage` parameter per call site because the service reuses the store's generic `ErrNotFound` for both "tweet not found" and "user not found" (in `ListByUsername`). Seven new routes wired into `handler.go`, all behind `requireAuth`. Tests: `validation.TweetContent` (trim/newlines-preserved/empty/280-vs-281 including a multi-byte-emoji pair pinning the D-13 code-point count); `internal/service/tweet` (validation, trim-on-create, soft-delete excludes from `Get`, non-author delete → `ErrForbidden`, missing-tweet operations → `ErrNotFound`, timeline includes own+followed excludes non-followed, like-twice-counts-once/unlike-removes, like/list on a missing tweet/user → `ErrNotFound`); `internal/httpapi` (create success/validation-400s/no-cookie-401, get-by-id success/404, delete-by-author→204-then-404, delete-by-non-author→403, delete-unknown→404, list-by-username excludes deleted/404s on unknown username, timeline include/exclude/ordering through the real follow endpoint, like/unlike through the real HTTP layer, like-on-deleted-or-missing→404). Per the explicit "pagination has no duplicates/gaps" requirement, one test walks the real `sample.json`'s `GET /api/timeline` twice (once at `limit=50`, once at `limit=3`, both looped to exhaustion via `nextCursor`) and asserts the id sequences match — alice's real sample timeline is ~99 tweets, past `MaxLimit=50`, so an earlier draft of this test that treated a single `limit=50` page as the complete ground truth was itself wrong and had to be fixed to loop both sides. Verified end-to-end: `gofmt -l .` clean; `go build ./...`/`go vet ./...` clean; full suite green; `go test ./internal/... -coverpkg=./internal/... -coverprofile -covermode=atomic` → **93.1 % total statements** (down slightly from Step 4's 93.6 %, expected — Step 6 is the dedicated coverage checkpoint; still well above the 85 % floor); ran the real binary and drove login/create/timeline/like/delete/get-after-delete-404 with `curl` against the seeded sample data, confirming counts and the D-52 error envelope.

---

## Step 6 — User search + backend coverage checkpoint

**Prompt:**

> Add `GET /api/search/users?q=` per D-25 — case-insensitive substring on username or displayName, `q` trimmed and 1–50 chars (empty → 400), limit 20, ordered by username — with tests (including a mixed-case query against the sample data). Then run `npm run test:coverage`, report the total, and add tests for any uncovered service/handler/store branches until `server/internal` meets the D-32 threshold. Confirm the README Runbook's sample credentials (`alice@example.com` / `Password123!`) work against the running API and note there that Phase-1 data resets on restart (D-66).

**Done when:** API returns a populated timeline for alice on a fresh start; coverage ≥ 85 % and recorded in the README.

**Rubric:** Functionality (search), Seed requirement, Testing (85 %+), Documentation.

_As executed (2026-09-18):_ `validation.SearchQuery` (1-50 code points, trimmed, empty/whitespace-only rejected — D-25). `social.Service.SearchUsers` (validates the query, then calls the existing `store.SearchUsers(query, SearchResultLimit)` — the store-level substring match and username-ascending ordering were already implemented in Step 2; `SearchResultLimit = 20` per D-25's "no pagination, max 20"). `internal/httpapi`: `handleSearchUsers` (`GET /api/search/users?q=`, reusing `writeSocialError` since search lives in the social service) and a `searchUsersResponse{ Items []searchResultResponse }` envelope (id/username/displayName only — no `isFollowedByMe`, since D-25 doesn't call for follow state in search results). Wired behind `requireAuth` per D-11. Tests: `validation.SearchQuery` (trim/empty/whitespace/50-vs-51 length); `social.SearchUsers` (substring match, empty-query validation error); `internal/httpapi` (the prompt's explicit "mixed-case query against the sample data" case — `q=aR` against the real `sample.json` matches `carol` and `oscar` by case-insensitive substring on username, ordered by username ascending; a fresh-store ordering test with `carl`/`carla` pinning the username-ascending tiebreak; empty query → 400 with a `q` field error; no-cookie → 401). Coverage checkpoint: ran `go test ./internal/... -coverpkg=./internal/... -coverprofile -covermode=atomic` → 93.2 % (before closing gaps) then inspected `go tool cover -func` for partial-coverage lines specific to Step 5's new code (`writeTweetError` 62.5 %, `handleTimeline` 63.6 %) and added two targeted tests to close them: an `explodingLikeCountStore` (mirroring the existing `explodingEmailStore`/`explodingTweetCountStore` pattern) proving an unexpected store error in `tweet.Service.viewOf` surfaces as a 500 with no leaked detail, and an invalid-cursor/invalid-limit test for `GET /api/timeline`. Final: **93.4 % total statements**, comfortably above the 85 % floor; did not chase the remaining partial lines further (test-helper `t.Fatalf` branches and a no-op `Error()` method — the same shape of residual gap already present in Steps 3-4's coverage). Confirmed the sample credentials and the restart-resets-to-sample Phase-1 behavior (D-66) against a freshly started API with `curl`: login as alice, `GET /api/timeline` populated on a fresh start, `GET /api/search/users?q=aR` returning carol/oscar, empty query → 400. Updated README.md: project-status banner (Step 6 done, 93.4 % coverage), Runbook step range and boot-log paragraph (now lists tweet/timeline/like/search routes), the sample-credentials section (added timeline and search `curl` examples plus an explicit note that credentials were checked against a freshly started API), and the `internal/service` line in Architecture. No new environment variables.

**Outcome:** Files created: none (search extends the existing `social` service/`users.go` handler rather than a new package). Files changed: `server/internal/validation/{validation.go,validation_test.go}` (added `SearchQuery`), `server/internal/service/social/{social.go,social_test.go}` (added `SearchUsers`, `SearchResultLimit`), `server/internal/httpapi/{users.go,users_test.go,handler.go}` (search handler/route), `server/internal/httpapi/tweets_test.go` (two coverage-closing tests), `README.md`, `IMPLEMENTATION-PLAN.md`, `my-docs/AGENT-COLLABORATION-HISTORY.MD`. No commits made (user commits manually); suggested boundaries: (1) `feat(server): add user search endpoint`, (2) `test: close Step 5 coverage gaps (tweet service error path, timeline validation)`, (3) `docs: record Step 6 completion, coverage 93.4%`.

---

## Step 7 — Frontend foundation + auth UI + first frontend integration test

**Goal:** mobile-first app shell and the full auth loop in the browser.

**Prompt:**

> In `client/`, build the mobile-first app shell per D-28: bottom tab bar on `<640px`, icon rail at `sm:`, labeled sidebar + `max-w-[600px]` content column at `lg:`. Set up a typed API client (relative `/api`, `credentials: 'include'`, unwraps the D-52 error envelope), TanStack Query provider, an `Avatar` component rendering initials on a username-hashed color (D-12), and an auth context backed by `GET /api/auth/me`. Pages: `/register` (with client-only confirm-password), `/login`, a `ProtectedRoute` wrapper that redirects to `/login`, and redirect-away-if-authenticated on the two public pages (D-11). Forms with client-side validation mirroring the server rules via `client/src/lib/validation.ts` (D-54) and server error display. Logout button in the nav. Configure Vitest + React Testing Library + MSW for `client/`, and write the login-flow integration test: renders form, submits valid credentials, mocked API returns user, app navigates to `/` and shows the username in the nav; plus an invalid-credentials error case.

**Done when:** register → login → logout works against the real Go API in the browser at all three breakpoints; client test suite runs green.

**Rubric:** Functionality (auth), Responsive design, Testing (frontend).

_As executed (2026-09-18):_ `client/src/lib/api.ts` (typed fetch wrapper — relative `/api/*`, `credentials: 'include'`, unwraps the D-52 envelope into a thrown `ApiError` carrying `status`/`code`/`details`) and `client/src/lib/validation.ts` (D-54 mirror: the same length bounds, username/email regexes and `countCodePoints` as `server/internal/validation`, used for register-form client-side checks — the reserved-username list stays server-only, per the trade-off now noted in the README). `client/src/context/AuthContext.tsx` (`AuthProvider`/`useAuth`: resolves the session once from `GET /api/auth/me` on mount, exposes `user`/`loading`/`setUser`/`logout`; `setUser` lets Login/Register adopt the user their own request already returned without a second round trip). `client/src/components`: `ProtectedRoute`/`PublicOnlyRoute` (D-11 redirect pair), `AppShell` + `AppShell.css` (D-28 responsive nav — mobile sticky top bar with a logout icon button + fixed bottom tab bar, tablet icon-only left rail, desktop labeled sidebar with a centered `max-width: 600px` content column; one `navItems` array renders both the rail and the bottom bar), `Avatar` + `Avatar.css` (D-12 initials-on-hashed-color placeholder, no network/image dependency), `AuthLayout` + `AuthLayout.css` (shared card/form chrome for Login and Register), and `icons.tsx` (four small inline SVGs — home/search/profile/logout — instead of an icon-library dependency). Pages: `Login.tsx`, `Register.tsx` (email/username/password/confirm-password/optional-displayName, confirm-password checked client-only per D-05, server field errors from `ApiError.details` rendered under the matching input), `NotFound.tsx` (catches nav links to `/search` and `/:username` before Steps 8–9 build them, so navigation never dead-ends blank). `App.tsx` rewired: `/login` and `/register` wrapped in `PublicOnlyRoute`, everything else wrapped in `ProtectedRoute` + `AppShell` with a nested `Routes` (`/` → `Home`, `*` → `NotFound`); `main.tsx` wraps the tree in `AuthProvider`. `Home.tsx` now greets `user.displayName` to prove the auth wiring end to end instead of showing static placeholder text. Testing infra: `vite.config.ts` switched to `vitest/config`'s `defineConfig` with a `test` block (`environment: 'jsdom'`, `setupFiles: ['./tests/setup.ts']`); `client/tests/` holds `setup.ts` (imports `@testing-library/jest-dom/vitest`, starts/resets/stops the MSW server), `mocks/{handlers,server}.ts` (default handler: `GET /api/auth/me` → 401, so tests start logged-out unless overridden), `test-utils.tsx` (`renderApp` — mounts the real `App` inside `QueryClientProvider` + `MemoryRouter` + `AuthProvider`, reused by every future frontend integration test per the README's `/tests` layout), and `integration/login.test.tsx` (the two required cases: valid credentials → lands on `/` with the username visible in the nav; invalid credentials → the server's "Invalid email or password" message rendered inline). `client/package.json` gained `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `msw`, `jsdom` as devDependencies and a real `test` script (`vitest run`, replacing the Step 1 placeholder). Two real bugs were caught by testing in an actual browser rather than trusting the unit/integration suite alone: (1) `POST /api/auth/logout` (no body) omitted the `Content-Type: application/json` header, which the server's D-56 `jsonOnly` middleware rejects unconditionally on every state-changing method — fixed by always sending that header, not only when a body is present; (2) `.auth-form__field input:invalid:not(:placeholder-shown)` painted a red border on the _empty, untouched_ login form because no `placeholder` attribute was set (so `:placeholder-shown` never matched and the exclusion never applied) — removed the rule since field-level error text already covers this. Verified end-to-end: `tsc -p tsconfig.json && vite build` clean; `eslint .` clean (one pre-existing-pattern warning: `react-refresh/only-export-components` on `AuthContext.tsx` exporting both the provider and the `useAuth` hook — a standard React context pattern, non-blocking, CI has no `--max-warnings`); `go vet ./...` clean; `npx vitest run` → 2/2 passing; `prettier --check` clean on all new/changed client files. Then drove the real stack in a headless Chromium browser (Playwright, installed for this check) against `npm run dev` (Go API :3000 + Vite :5173): unauthenticated `/` → redirect to `/login`; register a unique user → redirect to `/` with the greeting and username visible in the nav at 375px; resized to 768px (icon rail) and 1280px (labeled sidebar, screenshots confirm the D-28 shape at each tier); logout → `/login`; log back in; authenticated visit to `/login` redirects to `/`; wrong password on the real API → "Invalid email or password" shown inline. Both browser bugs above were found and fixed during this pass, then re-verified with a clean run.

---

## Step 8 — Timeline, compose tweet, delete, like (frontend + test)

**Prompt:**

> Build the home timeline at `/`: `useInfiniteQuery` against `/api/timeline` with an intersection-observer sentinel plus a visible "Load more" fallback button (D-20), loading/error states, and the D-18 empty state linking to `/search`. Compose box at the top with a live code-point counter (D-13) that disables submit when over limit or empty; optimistic insert on success. `TweetCard` component: initials avatar, display name, `@username`, relative time with absolute `title` (D-30), content rendered `whitespace-pre-wrap` with `@mentions` linkified (D-14), like button with count (optimistic toggle, D-31), reply icon with count, delete action visible only for the author's tweets with a confirm. Write the create-tweet integration test: type content, counter updates, submit, mocked POST called with the content, tweet appears at the top; plus a test that the submit button is disabled at 281 chars.

**Done when:** alice's sample timeline renders and scrolls; compose/like/delete all work against the real API.

**Rubric:** Functionality (tweets, timeline, infinite scroll, likes), Testing (frontend), Responsive design.

_As executed (2026-09-18):_ `client/src/lib/api.ts` gained the tweet wire types (`Tweet`/`TweetAuthor`/`TweetPage`) and `getTimeline`/`createTweet`/`deleteTweet`/`likeTweet`/`unlikeTweet`, matching `server/internal/httpapi/tweets.go`'s `tweetResponse`/`tweetListResponse` shapes exactly. `client/src/lib/time.ts` (`formatRelativeTime`/`formatAbsoluteTime`, D-30: `5m`/`2h`/`3d` then a short date, absolute time for the card's `title` tooltip). `ComposeBox.tsx`/`.css` (live counter via `validation.ts`'s `countCodePoints`, disabled when trimmed-empty or over 280, clears only on success). `TweetCard.tsx`/`.css` (`Avatar`, display name/`@username`/relative time, `white-space: pre-wrap` content with `@mentions` linkified by a plain regex to `/:username` — D-14, no backend mention model — a reply icon+count linking to the not-yet-built `/tweet/:id` (resolves to the existing `NotFound` catch-all until Step 10), a like button, and an author-only delete button gated behind `window.confirm` rather than a custom two-click confirm state). `icons.tsx` gained `ReplyIcon`/`LikeIcon`. `Home.tsx` rewritten: `useInfiniteQuery` against the timeline, an `IntersectionObserver` sentinel plus a "Load more" button fallback (D-20), loading/error/D-18-empty states, and three `useMutation`s (create/like/delete) that patch the `['timeline']` query cache directly — create prepends to the first page, like/delete use `onMutate`/`onError` for optimistic-update-with-rollback (D-31) via a shared `mapTimeline` helper. `Home.css` rewritten for the real layout. Test: `client/tests/integration/compose-tweet.test.tsx` (type→counter updates→submit→mocked POST called with trimmed content→tweet appears at the top; submit disabled at 281 chars, enabled at 280). Found and fixed a real test-infrastructure bug while writing it: `client/tests/setup.ts` never called RTL's `cleanup()` between tests — `vite.config.ts` doesn't set `test.globals: true`, so RTL's normal auto-cleanup (which needs a global `afterEach`) never registered, and Step 7's `login.test.tsx` had only avoided the symptom because each of its two tests happened to navigate off the page the next one asserted against. Fixed at the root (`tests/setup.ts`), not in the one test that exposed it. `client/tests/mocks/handlers.ts` gained a default empty-timeline `GET /api/timeline` handler (Home now fetches it on every authenticated mount); `login.test.tsx`'s stale "Welcome, {name}" heading assertion (pinned to the Step 7 placeholder `Home.tsx`, gone in this rewrite) was dropped in favor of asserting the compose box renders. Verified end-to-end: `tsc -p tsconfig.json`/`vite build`/`eslint .`/`prettier --check` all clean; `npx vitest run` → 4/4 passing; `go build ./...` unaffected. Then ran the real stack (`go run ./cmd/api` + `vite`) and drove it with a headless Chromium/Playwright script logged in as alice: the real 20-tweet first page of her sample timeline rendered; composed and posted a unique tweet (counter updated live, appeared at the top on success); liked it (count 0→1 optimistically); deleted it via the confirm dialog (removed from the list with no reload); screenshotted 375/768/1280px confirming the compose box and card list stay inside the D-28 content column at every tier. Both dev-server processes stopped afterward.

---

## Step 9 — Profile pages, follow/unfollow, search (frontend + test)

**Prompt:**

> Add `/:username` profile page: header with placeholder avatar, display name, `@username`, bio, follower/following counts linking to `/:username/followers` and `/:username/following` list pages (paginated), a Follow/Unfollow button (hidden on own profile; optimistic toggle updates counts), an "Edit bio" inline form on own profile, and the user's tweets below with infinite scroll. Add `/search` with a debounced input hitting `/api/search/users` and a result list linking to profiles; put a search entry in the nav. Write the follow-flow integration test: render another user's profile, click Follow, mocked POST called, button flips to Following and follower count increments; and the reverse for Unfollow.

**Done when:** all three required frontend flows (login, create tweet, follow) have passing integration tests; every required feature in the brief §4 is usable end to end.

**Rubric:** Functionality (profile, follows, lists, search), Testing (frontend).

_As executed (2026-09-18):_ Extended `client/src/lib/api.ts` with the profile/follow/search wire types and endpoints (`Profile`, `getProfile`, `updateProfile`, `follow`/`unfollow`, `FollowListItem`/`getFollowers`/`getFollowing`, `SearchResult`/`searchUsers`, `getUserTweets`), matching `server/internal/httpapi/users.go`'s response shapes exactly — including the gotcha that `PATCH /api/users/me` isn't a true partial update (`updateProfileRequest` always writes both fields), so `updateProfile` always sends the current `displayName` alongside a bio-only edit. Added `validateBio` to `validation.ts` (D-54 mirror of the server's `Bio` rule). Refactored Home's inline infinite-scroll/like/delete logic (from Step 8) into two small reusable pieces, since Profile needed the identical behavior for its own tweet list: `client/src/lib/useInfiniteScrollSentinel.ts` (the D-20 IntersectionObserver-plus-button pattern, generic over any paginated list) and `client/src/lib/useTweetFeed.ts` (infinite tweet list + optimistic like/delete-with-rollback, D-19/D-31, built on the sentinel hook), plus a shared `client/src/components/TweetList.tsx` presentational component (loading/error/empty/list/load-more) used by both `Home.tsx` (now much shorter) and the new `Profile.tsx`. Built `Profile.tsx`/`.css` (`/:username`): avatar/displayName/@username/bio, an inline "Edit bio" form on own profile only (`validateBio` client-side, sends the unchanged `displayName` per the PATCH gotcha above), follower/following counts linking to the list pages, a Follow/Following toggle button (hidden on own profile) with optimistic update + rollback + server-truth reconciliation on the `['profile', username]` query, and the user's tweets below via `useTweetFeed`/`TweetList`. Built `FollowList.tsx`/`.css` (`/:username/followers` and `/:username/following`, one component parameterized by `mode`): D-24 rows (avatar, display name, `@username`, bio, per-row follow toggle hidden for self), cursor-paginated with the same sentinel-plus-button pattern. Built `Search.tsx`/`.css` (`/search`): a 300ms-debounced input (D-26) against `GET /api/search/users`, results linking to profiles; the nav's existing Search link (already wired in Step 7's `AppShell`) now resolves to a real page. Wired all four routes into `App.tsx` (`/search`, `/:username`, `/:username/followers`, `/:username/following`) — React Router v6 ranks static segments over dynamic ones, so `/search` matches before the `/:username` catch-almost-anything route regardless of declaration order. Test: `client/tests/integration/follow-flow.test.tsx` (both required cases — click Follow on another user's profile → mocked `POST /api/users/bob/follow` called → button flips to "Following" and the follower count increments; click Following → mocked `DELETE` called → flips back to "Follow" and the count decrements). `client/tests/mocks/handlers.ts` gained a default empty-page handler for `GET /api/users/:username/tweets` (every profile page fetches it, same rationale as Step 8's timeline default). Verified end-to-end: `tsc -p tsconfig.json`/`vite build`/`eslint .`/`prettier --check` all clean; `npx vitest run` → 6/6 passing (2 login, 2 compose, 2 follow-flow); `go build ./...` unaffected. Then ran the real stack and drove it with headless Chromium/Playwright scripts logged in as alice against the real sample data: searched for "heidi", opened her real profile, followed her (followerCount 4→5, server-confirmed, button flipped to "Following"), unfollowed her (5→4, button back to "Follow") — an early debugging pass had appeared to show a follower-count regression, which turned out to be a bug in the _debug script_ (Playwright's `get_by_role(name=...)` substring-matches "Follow" against the "Following" button, unlike Testing Library's exact-by-default matching used in the actual test suite), not the app; re-verified with precise CSS-class locators and confirmed the app's counts and button state are correct on every click. Also edited alice's own bio inline and confirmed it persisted and rendered; visited `/alice/followers` and confirmed all 11 real sample followers rendered with correct per-row Follow/Following state matching the real follow graph; screenshotted 375px and 1280px. Both dev-server processes stopped afterward.

---

## Step 10 — Bonus: reply threads (backend + frontend + tests)

**Prompt:**

> Implement reply threads per D-47…D-50. Backend: `POST /api/tweets/{id}/replies` (same content rules, sets `parentTweetId`, 404 if parent missing or deleted), extend `GET /api/tweets/{id}` to return `{ ancestors, tweet, replies: { items, nextCursor } }` (ancestors walked to the root capped at 50, direct replies only, `createdAt ASC`), and make sure `replyCount` excludes deleted replies. Frontend: tweet detail page at `/tweet/:id` showing the ancestor chain (deleted ancestors as a "This tweet was deleted" placeholder — D-49), the focused tweet, an inline reply composer, and the paginated replies list; reply cards show a "Replying to @user" line. Tests: backend integration tests for reply creation, 404 on missing/deleted parent, thread retrieval order, deleted-ancestor placeholder data, and timeline exclusion (the sample data already contains nested replies to assert against); one frontend test that the detail page renders the thread and posting a reply appends it.

**Done when:** a multi-level thread can be created and navigated in the browser; tests green; backend coverage still ≥ 85 %.

**Rubric:** Bonus, Testing.

_As executed (2026-09-18):_ the store layer (`ListReplies`, `Ancestors`, the D-49 deleted-row exception, `ReplyCount` excluding deleted replies) was already built and conformance-tested in Step 2, so this step was the service, HTTP and frontend layers on top of it. `internal/service/tweet`: `Service.CreateReply(authorID, parentTweetID, content)` (same D-13/D-14 validation as a top-level tweet, `store.ErrNotFound` if the parent is missing or deleted per D-47) and `Service.GetThread(viewerID, tweetID, cursor, limit)` returning a new `Thread{ Ancestors []View, Tweet View, Replies store.Page[View] }` — ancestors are built by running the same `viewOf` used everywhere else over `store.Ancestors`' rows (deliberately including deleted ones, per the Step 2 exception), so a deleted ancestor's `View` still carries real author/content data with `Tweet.IsDeleted() == true` rather than being redacted server-side; `MaxAncestorHops = 50` (D-48 safety valve). `internal/httpapi/tweets.go`: `tweetResponse` gained `isDeleted` (computed from `Tweet.IsDeleted()`, false everywhere except a deleted thread ancestor); `handleCreateReply` (`POST /api/tweets/{id}/replies`); `threadResponse{ Ancestors, Tweet, Replies }` and `handleGetThread`, which **replaces** the old `handleGetTweet` on `GET /api/tweets/{id}` — that route now always returns the D-48 thread shape instead of a bare tweet, with `cursor`/`limit` on the query string pagination the replies exactly like every other list endpoint (D-19). Two new routes wired into `handler.go`. Tests: `internal/service/tweet` (reply creation trims/validates/sets `ParentTweetID`, reply to missing/deleted parent → `ErrNotFound`, `GetThread` returns ancestors root-first + the focused tweet + its replies, deleted focus → `ErrNotFound`, and a dedicated test proving a deleted ancestor's data still reaches the service layer with `IsDeleted() == true`); `internal/httpapi` (reply creation success/400/404/401, `GET /api/tweets/{id}` returning the full thread shape with ancestors root-first and replies in `createdAt ASC` conversation order across a real three-level thread, deleted-focus → 404, deleted-ancestor placeholder data (`isDeleted: true`) reachable through a live reply, replies excluded from both the timeline and profile tweet list, and `replyCount`/thread replies excluding a deleted reply). `TestGetTweetByID` (Step 5) updated to decode the new `threadResponse` shape instead of a bare `tweetResponse`, since it exercises the same route. Frontend: `client/src/lib/api.ts` gained `Tweet.isDeleted`, a `Thread` type mirroring `threadResponse`, and `getThread`/`createReply`. `client/src/lib/useThread.ts` (new hook, structurally mirroring Step 9's `useTweetFeed`: an infinite query over thread pages keyed by reply cursor, optimistic like/unlike on the focused tweet and any reply via a shared toggle mapped over both `page.tweet` and every page's `replies.items`, reply deletion, and `appendReply` for the composer). `client/src/pages/TweetDetail.tsx`/`.css` (new page at `/tweet/:id`, wired into `App.tsx` alongside the existing `/search`/`/:username` routes): the ancestor chain rendered as compact clickable rows (a deleted one as the D-49 "This tweet was deleted" placeholder instead), the focused tweet and each reply reusing the existing `TweetCard` component (so like/delete/reply-navigation all come for free), a "Replying to @{focusedAuthor}" line above each reply per D-50 (correct because every item in the D-48 `replies` page is, by construction, a _direct_ reply to the focused tweet), an inline `ComposeBox` reusing Step 8's component, and the same sentinel-plus-button infinite-scroll pattern as every other list. Deleting the tweet currently being viewed navigates home rather than trying to keep the thread view in sync (out of scope for this step; no plan requirement called for it). Test: `client/tests/integration/thread.test.tsx` (renders ancestors/focused tweet/replies from a mocked `GET /api/tweets/:id`, posting a reply calls the mocked `POST /api/tweets/:id/replies` with the trimmed content and appends it to the list; a second case asserts the deleted-ancestor placeholder renders and the live tweet's own content does not leak into a deleted ancestor's row). A real CSS bug was caught only by driving the app in an actual browser (not by the test suite, which doesn't assert on list markers): `.tweet-detail__replies` is a `<ul>` and, unlike every other list in the app (`.tweet-list` in `TweetList.css`), didn't reset `list-style`, so a bullet point rendered next to the reply's "Replying to" line — fixed by adding the same `list-style: none` reset. Verified end-to-end: `go build ./... && go vet ./... && gofmt -l .` clean; full Go suite green; `go test ./internal/... -coverpkg=./internal/... -coverprofile -covermode=atomic` → **93.2 % total statements** (down marginally from Step 9's implicit baseline, still comfortably above the 85 % floor); `tsc -p tsconfig.json && vite build` clean; `eslint .` clean (same pre-existing `AuthContext.tsx` warning noted since Step 7); `prettier --check` clean; `npx vitest run` → **8/8 passing** (2 login, 2 compose, 2 follow-flow, 2 thread). Then ran the real stack (`npm run dev`) and drove a full multi-level scenario with a headless Chromium/Playwright script logged in as alice against the real API: composed a root tweet, opened its thread via the reply icon, posted a level-1 reply, opened _that_ reply's own thread page (confirming the root now renders as a clickable ancestor row), posted a level-2 reply (confirming the "Replying to @alice" line), navigated back up the ancestor chain to the root's thread, deleted the root tweet from there (confirming the app redirects home), then revisited the level-1 reply's thread page directly by URL and confirmed the root now renders as the D-49 "This tweet was deleted" placeholder while the reply itself remains fully reachable — screenshotted at 375px (bottom tab bar, no horizontal overflow) and 1280px (labeled sidebar). Both dev-server processes stopped afterward.

---

## Step 11 — SQLite store, seed command, switch the default (Phase 2)

**Goal:** satisfy the brief's relational-database requirement with the same API and tests — a pure addition behind the `Store` interface (D-66).

**Prompt:**

> Add `internal/store/sqlite`: `database/sql` + `modernc.org/sqlite` (pure Go), an embedded `schema.sql` with the D-64 table shapes (snake_case tables, composite PKs on `follows`/`likes`, indexes `(author_id, created_at DESC, id DESC)`, `(followee_id, created_at DESC)`, `(follower_id, created_at DESC)`, `(parent_tweet_id, created_at)`, `(tweet_id)`, the self-follow `CHECK` inside `CREATE TABLE follows`, `ON DELETE NO ACTION` on reply → parent, `PRAGMA foreign_keys = ON` on every connection, WAL mode) applied at open via `CREATE TABLE IF NOT EXISTS` + a `schema_version` row, and every `Store` method as parameterized SQL (search per D-25 amendment, counts via `COUNT(*)`, cursor pagination via `(created_at, id) < (?, ?)`). Run the `storetest` conformance suite and the `httpapi` integration tests against it on a `t.TempDir()` file. Add `cmd/seed` (D-39/D-67: truncate then insert from `sample.json`; refuses under `APP_ENV=production` unless `SEED_FORCE=true`) and the root script `npm run seed`. Wire `SQLITE_PATH` (default `./data/twitter.db`, gitignored) and `SEED_FORCE`; flip the `STORE` default to `sqlite`; `cmd/api` seeds automatically when the `users` table is empty so `npm run dev` on a fresh clone is populated. Update `.env.example`, D-43, the README (Runbook now: `cp .env.example .env`, `npm install`, `npm run dev`; `npm run seed` to reset the data; the restart-resets note becomes the memory-store-only note), and the trade-offs section.

**Done when:** the whole suite is green on both stores; a fresh clone + Runbook yields a seeded SQLite app; `git status` shows no `.db` files.

**Rubric:** Functionality, Testing, Documentation — and the brief's §2 database requirement.

_As executed (2026-09-18):_ `internal/store/sqlite` implements `store.Store` on `database/sql` + `modernc.org/sqlite` (pinned at `v1.38.2` — the newest version whose own `go.mod` still declares `go 1.23.0`, so `server/go.mod`'s minimum did not need to bump past D-65's floor; `go get @latest` would have pulled `v1.59.0` and forced `go 1.25.0`). `schema.sql` (embedded via `go:embed`) carries the D-64 shapes verbatim: snake_case tables, TEXT UUID ids, the self-follow `CHECK` inside `CREATE TABLE follows`, `ON DELETE NO ACTION` from a reply to its parent, and the five indexes the prompt lists. `sqlite.Open` applies the schema with `CREATE TABLE IF NOT EXISTS`, seeds a `schema_version` row once, and turns on `foreign_keys`/`WAL`/a 5 s `busy_timeout` via DSN pragmas (`file:path?_pragma=...`, confirmed with a throwaway smoke program before wiring it in). One deliberate simplification beyond the prompt: the connection pool is capped at `SetMaxOpenConns(1)` — D-64's own text ("one writer at a time is fine at this scale") already licenses this, and it sidesteps `SQLITE_BUSY` without a retry loop. `created_at`/`updated_at`/`deleted_at` are stored as a fixed-width, zero-padded RFC 3339 `TEXT` (`...15:04:05.000000000Z`, not Go's trimmed `RFC3339Nano`) so lexicographic `ORDER BY`/`(created_at, id) < (?, ?)` cursor comparisons are trivially correct instead of relying on an argument about trimmed-fraction string ordering. Every `Store` method is hand-written parameterized SQL, one file per Step-2 grouping (`users.go`/`follows.go`/`tweets.go`/`likes.go`) mirroring `store/memory`'s split; list methods fetch `limit+1` rows and a shared `trimPage` helper computes the next cursor, the SQL equivalent of `memory/paginate.go`'s `slicePage`. `Follow`/`Like` use `INSERT ... ON CONFLICT DO NOTHING` for idempotency (D-21/D-22); `Ancestors` walks the parent chain one row at a time in Go rather than a recursive CTE, capped at `maxHops`, and deliberately does not filter `deleted_at` (D-49). A real bug caught by the conformance suite, not written in from a spec: the first `CreateUser` draft mapped any UNIQUE-constraint error to a single generic `"user %q"` message, but `service/auth.conflictField` recovers which field conflicted by string-matching `"email"` in that message (to build the D-52 per-field `details`) — `TestRegisterDuplicateEmailReturns409` failed under `HTTPAPI_TEST_STORE=sqlite` with the email conflict reported as a username conflict. Fixed by having `CreateUser` check username/email existence explicitly before inserting (two `SELECT`s, cheap at registration's frequency) rather than parsing SQLite's driver-internal constraint-error text, so the field-specific `store.ErrConflict` wrapping matches `store/memory`'s exactly. `cmd/seed/main.go`: loads config, refuses under `APP_ENV=production` without `SEED_FORCE=true`, truncates (`Store.Truncate`, FK-safe order: likes → follows → tweets → users) then calls the same `store/sample.Load` the memory store and `cmd/api` already used — no seed-specific insert logic. `cmd/api/main.go`'s `buildStore` gained a `StoreSQLite` case: opens the file, and seeds from `sample.json` only when `Store.IsEmpty()` is true, so a fresh clone boots populated and a later restart keeps whatever the API has since persisted. `internal/config`: `StoreSQLite`, `DefaultSQLitePath` (`./data/twitter.db`), `SQLitePath`/`SeedForce` fields, `SQLITE_PATH`/`SEED_FORCE` env parsing; **the `STORE` default flipped from `memory` to `sqlite`** (D-66), which is an intentional breaking change to two existing `internal/config` tests, updated in the same commit rather than left red. Per the prompt's explicit "run the httpapi integration tests against it," `internal/httpapi`'s eleven scattered `memory.New()` call sites (across `auth_test.go`, `context_test.go`, `ratelimit_test.go`, `tweets_test.go`, `users_test.go`) were replaced with one shared `newStore(t)` helper in `handler_test.go` that returns a memory store by default or, when `HTTPAPI_TEST_STORE=sqlite` is set, a real SQLite file in `t.TempDir()` — so the whole `internal/httpapi` suite runs unmodified against either backend, run explicitly both ways locally and wired as a second CI step (`HTTPAPI_TEST_STORE=sqlite go test ./internal/httpapi/...`) alongside the existing `go test -race ./...`. The three `exploding*Store` test decorators (`explodingEmailStore`, `explodingLikeCountStore`, `explodingTweetCountStore`) switched from embedding the concrete `*memory.Store` to embedding the `store.Store` interface — Go promotes the embedded interface's methods the same way it promotes a concrete embedded type's, so this was a one-line change per decorator, not a rewrite. CI's server job also picked up a pre-existing, now-glaring staleness fix: `cache: false` (left over from "no third-party modules yet, so there is no go.sum" in the Step 1 scaffold) became `cache-dependency-path: server/go.sum`, now that real dependencies exist. `.gitignore` gained `*.db-wal`/`*.db-shm` alongside the pre-existing `*.db` (WAL mode's sidecar files use a different suffix pattern). Verified end-to-end: `gofmt -l .` clean; `go build ./...`/`go vet ./...` clean; `go test ./...` green (the sqlite conformance suite plus two dedicated `sqlite`-package tests — reopening a file idempotently applies schema.sql without error, and `Truncate` genuinely empties `IsEmpty`); `go test ./internal/... -coverpkg=./internal/... -coverprofile -covermode=atomic` → **91.5 % total statements**, above the 85 % floor (down from Step 10's 93.2 % — expected, an entire new store implementation landed with the same "don't chase every defensive branch" policy as prior steps, e.g. the near-unreachable race-fallback branch in `CreateUser`). Ran the real binary three times by hand: `rm data/twitter.db* && npm run seed` → `seeded ./data/twitter.db: users=12 tweets=169 follows=63 likes=499`; fresh boot with no `.db` file present → boot log `seeded empty sqlite store` with the same counts, `GET /api/health` 200; registered/logged in, posted a tweet via `curl`, confirmed it in `GET /api/timeline`, killed and restarted the process → boot log `opened sqlite store` (not re-seeded) and the same tweet still present in the timeline, proving persistence across restarts where the memory store would have reset. `git status` after all of the above shows no `.db`/`.db-wal`/`.db-shm` files. `.env`/`.env.example`/`my-docs/DECISIONS.md` D-43 (the Step-11 row was already forward-drafted and needed no edit) updated; README Runbook, environment-variable table, sample-credentials restart note, and Known Trade-offs section updated to describe the sqlite-by-default, memory-is-now-opt-in state.

---

## Step 12 — E2E auth test (Playwright), responsive QA, coverage proof, final documentation

**Prompt:**

> (1) Add Playwright at the repo root (`/e2e`) with one spec covering the auth flow against an already-running stack at `http://localhost:5173` (D-36): visit `/`, get redirected to `/login`, register a unique `e2e_<timestamp>` user, land on the timeline with the username visible, log out, verify `/` redirects to `/login` again, log back in; add `npm run test:e2e` and document that the app must be running first. (2) Responsive QA: walk every page at 375px, 768px and 1280px; fix overflow, cramped touch targets, desktop-first leftovers. (3) Run the full suite — server coverage on both stores, client tests, E2E — and fix anything red; put the final server coverage number in README. (4) Runbook verification: follow README's Runbook from a fresh clone literally (Go + Node only), fix any step that needs undocumented intervention, remove remaining "Planned" markers. (5) Fill README's Known Trade-offs with the real decisions (COUNT-on-read, single JWT cookie, replies hidden from timeline, phased persistence and the memory store, Docker deferred, anything discovered). (6) `gofmt`/`go vet`/lint clean, remove dead code and debug logs, make sure `.env.example` lists every variable the code reads. Commit as a few small polish/docs commits, not one blob.

**Done when:** README is fully accurate, all suites green, coverage ≥ 85 % recorded, working tree clean, `main` pushed.

**Rubric:** Testing (E2E requirement), Documentation, Code quality, Development process — and protects the Functionality score by guaranteeing the Runbook works.

_As executed (2026-09-18):_ (1) Added `@playwright/test` as a root devDependency (Chromium browser already present locally from earlier steps' manual browser QA). `e2e/playwright.config.ts` (`testDir: '.'`, `baseURL: http://localhost:5173`, one `chromium` project, deliberately **no `webServer` entry** per D-36 — the spec does not start the app). `e2e/auth.spec.ts`: registers a unique `e2e_<Date.now()>` user (fits D-03's 3-20-char `^[a-zA-Z0-9_]+$` username rule), then drives register → lands on `/` with the username visible in the nav → clicks the accessible-name "Log out" button → redirected to `/login` → logs back in with the same credentials → lands on `/` again. Root `npm run test:e2e` script added; README documents that the app must already be running. Ran it for real against a live `npm run dev`: passed first try after one prerequisite fix (next item).
(2) Responsive QA: booted the real stack and, with a throwaway Playwright/Python script (not committed — scratch-directory tooling per D-69's bulk-mechanical exception), logged in as alice and screenshotted every route (`/`, `/search`, `/:username`, `/:username/followers`, `/:username/following`, `/tweet/:id`, `/login`, `/register`, and an unknown-username route) at 375/768/1280px, plus checked `scrollWidth` vs `clientWidth` at each to catch horizontal overflow programmatically rather than eyeballing every screenshot. Found and fixed two real bugs: **(a)** in `AppShell.tsx`, the icon-rail and bottom-bar `NavLink`s and the rail's logout button rendered their label as a `<span>` that CSS hides (`display: none`) at the tablet breakpoint (640–1023px) and, for the rail, also hides its label at 640-1023px — since the icons are `aria-hidden`, this left every nav item and the rail logout button with **no accessible name** in that range (a real screen-reader gap, not just a visual one); fixed by adding an explicit `aria-label={label}` to each, which is harmless at breakpoints where the visible label already exists. **(b)** `.follow-list__follow-btn` (the per-row Follow/Following button on `/:username/followers` and `/:username/following`) was `min-height: 36px`, below the 44px touch-target minimum every other interactive control in the app already uses (`.tweet-card__action`, `.profile__follow-btn`, `.follow-list__load-more`, `.app-shell__logout` are all 44px) — fixed to match. No horizontal overflow found on any page at any breakpoint.
(3) Full suite run: `go build`/`gofmt -l`/`go vet` clean; `go test ./...` green on the default in-memory store; `HTTPAPI_TEST_STORE=sqlite go test ./internal/httpapi/...` green; `go test ./internal/... -coverpkg=./internal/... -coverprofile` → **91.5 % total statements**, unchanged from Step 11 (no new server code landed this step) and still well above the 85 % floor (D-32); `npx eslint .`/`prettier --check .`/`tsc && vite build` all clean (the one pre-existing `react-refresh/only-export-components` warning on `AuthContext.tsx`, noted since Step 7, is unchanged); `npx vitest run` → 8/8; `npm run test:e2e` → 1/1. While driving the browser for the responsive pass, also caught (via the "unknown username" screenshot sitting on "Loading profile…" far longer than the others) a real UX bug unrelated to layout: `client/src/main.tsx`'s `QueryClient` used TanStack Query's default retry policy, which retries **every** failed query 3 times with exponential backoff — including a definite 404. Visiting a nonexistent username sat on "Loading profile…" for **~7.6 s** before showing "User not found." Fixed at the `QueryClient` level (`defaultOptions.queries.retry`), not in the one query that exposed it: don't retry when the thrown error is an `ApiError` with a 4xx status, since a client error is never transient. Reproduced with a scratch Playwright timing script before and after: 7.62 s → 0.16 s.
(4) Runbook verification: a literal `git clone` into a scratch directory wasn't a meaningful test yet — per D-61/the working agreement, the user commits manually in VS Code, and as of this step nothing beyond the Step 1 scaffold (reverted and re-committed as `42bcbce`/`8970872`) is actually in `git log`, so a fresh clone today would only reproduce the bare scaffold, not the 11 steps of uncommitted work this Runbook describes. Instead verified the equivalent in place: stopped the running dev server, deleted `server/data/twitter.db*` to reproduce a fresh clone's "no database file yet" state, confirmed `.env` is byte-for-byte what `cp .env.example .env` would produce, then ran `npm run dev` and got the exact boot log the README documents (`seeded empty sqlite store ... users=12 tweets=169 follows=63 likes=499`); ran every curl command in the README's "Sample credentials" section verbatim against the freshly seeded API (health, login, me, profile, timeline, search) and all matched; ran `npm run seed` against the live server afterward and confirmed the API kept serving correctly (SQLite's `SetMaxOpenConns(1)` plus WAL mode tolerates a separate short-lived writer process). No undocumented intervention was needed; removed the two remaining "Planned" markers (the E2E section, the project-status banner). **Real fresh-clone verification (a second machine, or `git clone` after the user's first round of commits) is still recommended before final delivery** — flagged to the user rather than silently claimed as fully proven.
(5) Filled in two more Known Trade-offs entries beyond the ones already present from earlier steps (session strategy, counts-on-read, private-by-default, soft deletes, in-memory dev option, single-writer SQLite, client validation as UX mirror, no Docker yet, no uploads/real-time/notifications): why the E2E suite is intentionally one spec (D-36 — the other required flows are covered faster and more reliably by the Vitest+MSW integration suite), and the React Query retry-policy fix from (3) above.
(6) `gofmt -l .`/`go vet ./...` clean; grepped the whole client and server trees for `console.log`/`debugger`/`fmt.Println`/stray `log.Println` outside tests and for `TODO`/`FIXME`/`XXX` markers — none found (this codebase came into Step 12 already clean, nothing to remove). Cross-checked `.env.example` against every `nonEmpty(lookup, ...)` call in `internal/config/config.go` — all eight variables (`PORT`, `APP_ENV`, `JWT_SECRET`, `COOKIE_SECURE`, `STORE`, `SAMPLE_DATA_PATH`, `SQLITE_PATH`, `SEED_FORCE`) were already listed; no gap to fix.

**Outcome:** Files created: `e2e/playwright.config.ts`, `e2e/auth.spec.ts`. Files changed: `package.json` (`@playwright/test` devDependency, `test:e2e` script), `client/src/main.tsx` (`QueryClient` retry policy), `client/src/components/AppShell.tsx` (`aria-label` on nav items and the rail logout button), `client/src/pages/FollowList.css` (follow-button touch target), `README.md` (project-status banner, architecture tree, E2E Runbook section, two new Known Trade-offs entries), `IMPLEMENTATION-PLAN.md` (this entry), `my-docs/AGENT-COLLABORATION-HISTORY.MD`. No commits made (user commits manually); suggested boundaries: (1) `test(e2e): add the Playwright auth-flow spec (D-36)`, (2) `fix(client): give hidden-label nav items an accessible name at the tablet breakpoint`, (3) `fix(client): follow-button touch target was under the 44px minimum`, (4) `fix(client): don't retry 4xx query errors (404 profile took ~7s to show an error)`, (5) `docs: record Step 12 completion — final coverage, trade-offs, Runbook verification`.

---

## Before delivery — final checklist

- [ ] Repo is public (or evaluators invited) — open item #1 in VALIDATION-OF-REQUIREMENTS.md
- [ ] All commits on `main`, none squashed — **nothing beyond the Step 1 scaffold is committed yet**; Steps 2–12 are verified in the working tree and awaiting the user's commits in VS Code (see each step's suggested commit boundaries)
- [x] README Runbook works with only Go 1.23+, Node 20+ and Git installed — verified against a reset local database in Step 12; a true `git clone` re-check is recommended once the above commits land
- [x] `STORE` default is `sqlite` and the SQLite store passes the full suite (open item #10) — done in Step 11
- [x] Sample credentials in README are valid against `sample.json` — re-verified in Step 12 against a freshly seeded database
- [x] Server coverage ≥ 85 % and stated in README — 91.5 % as of Step 11, unchanged through Step 12
- [x] Three frontend integration tests (login, create tweet, follow) + one E2E auth spec present and passing — 8/8 Vitest (including the bonus thread test) + 1/1 Playwright
- [ ] Decide whether `my-docs/` and `CLAUDE.md` ship in the repo (currently gitignored) — see [CLAUDE.md](../CLAUDE.md)
- [x] AGENT-COLLABORATION-HISTORY.MD has an entry for every prompt used

---

## Post-MVP backlog (only after every step above is done)

### B-1 — Docker Compose full stack (bonus; D-63, D-40/D-41/D-46 as amended)

> Complete the Docker Compose bonus: services `server` (two-stage `golang:1.23-alpine` → static `CGO_ENABLED=0` binary in a minimal runtime image, `sample.json` included, a named volume for the SQLite file, entrypoint seeds only if `users` is empty, `GET /api/health` healthcheck) and `client` (Vite build served by nginx on host 5173, proxying `/api` to `server:3000`), with healthchecks, `depends_on: condition: service_healthy`, and `.dockerignore`. `docker compose up --build` from a clean clone must produce a seeded, working app with no other steps. Add a README Runbook "Option A — Docker Compose" that matches exactly what you built.

Starting material: `my-docs/archive/docker/` (a postgres-era compose file, now only useful for the healthcheck/volume shape).

**Done when:** `git clone` → `cp .env.example .env` → `docker compose up --build` → log in with the sample credentials works on a machine with only Docker installed.

### Dropped (2026-09-16, D-65)

B-2 (switch to PostgreSQL) is no longer a backlog item: SQLite is the final database. `my-docs/archive/postgres/` is kept only as a DDL reference for `schema.sql`.
